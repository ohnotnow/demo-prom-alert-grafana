apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''                # top‐level folder in Grafana UI
    type: file
    disableDeletion: false    # if true, dashboards removed from the filesystem won’t be deleted in Grafana
    editable: false
    options:
      path: /var/lib/grafana/dashboards


